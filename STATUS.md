# Release status

Current status: **Candidate** — E2E fine-tuning tutorial source complete, awaiting supported clean-runtime execution. The pipeline package, 49 offline unit tests (including the notebook-parity tests) plus 3 model-backed tests, one executed local CPU run of the committed notebook blob, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/aurora_earth_system_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

No licence or remote-code gate is open for DIMER hosting (MIT weights; the model class comes from `microsoft-aurora` on PyPI). Two review items are open on the asset side: both upstream files are pickles, which this repository audits statically and converts once — through torch's weights-only unpickler and a two-name `find_class` allow-list — into digest-pinned safetensors that are the only artifacts the runtime path loads; whether that meets the DIMER deserialization-trust bar, or whether DIMER should host only maintainer-converted files, is the maintainer's call. And the tutorial runs the 0.25° model at 1.5°, an out-of-distribution use; which resolution tier a hosted profile should serve is a second call. See `MODEL_CARD.md` (*DIMER deployment notes*) and `docs/WEIGHTS.md`.
